# Linux Module Overzicht
type: atlas
domain: Linux
tags: #linux #atlas #overzicht
---
Overzicht van de basisconcepten van Linux (gebaseerd op lessen 1-5).

## Kernmodules
- [[Linux Index|Terug naar index]]

### 2. Shell Basis
- Terminal als primaire interface.
- Filesystem hiërarchie (`/`, home directory).
- File permissions en `man` commando.

### 3. Shell Omgeving & Historie
- Environment variables (`PATH`, `HOME`).
- `history` voor efficiëntie.
- Shell configuratie bestanden (`.bashrc`, etc.).
- `export` voor subproces variabelen.

### 4. Redirection & Pipes
- Redirection (`>`, `>>`) voor input/output controle.
- Pipes (`|`) voor commandokoppelingen.
- Streams (`stdin`, `stdout`, `stderr`) begrijpen.

### 5. Echo, Alias & Operators
- Quoting regels voor input interpretatie.
- Aliases voor tijdswinst.
- Control operators (`&&`, `||`) voor workflows.
- Exit status codes voor controle.
